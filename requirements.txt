pillow>=10.0.0
pywin32>=306; platform_system=="Windows"

# Linux (Kali): overlay window + global hotkeys (X11)
PySide6>=6.6; platform_system=="Linux"
pynput>=1.7.7; platform_system=="Linux"
# Nota: 'tkinter' NO se instala con pip; viene con Python si tienes instalado el componente Tcl/Tk.